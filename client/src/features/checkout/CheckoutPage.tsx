import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant="h3">
            Only logged in users shold be able to see this!
        </Typography>
    )
}